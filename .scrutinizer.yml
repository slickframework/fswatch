filter:
  excluded_paths:
    - 'tests/*'
checks:
  php: true

build:
  image: default-jammy
  environment:
    php: 8.2.15
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run
          - command: phpcs-run
            use_website_config: true
          - command: ./vendor/bin/phpunit
            coverage:
              file: coverage.clover
              format: clover